{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gradlew bootRun",
            "type": "shell",
            "command": "./gradlew bootRun",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Run Spring Boot application using Gradle",
            "options": {
                "cwd": "${workspaceFolder}/backend"
            }
        }
    ]
}
